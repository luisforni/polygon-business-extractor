# Este archivo no se usa directamente — es solo referencia.
# El único .env que Docker necesita es: packages/extractor-api/.env
# Copiarlo y completar las keys que quieras usar:
#
#   cp packages/extractor-api/.env.example packages/extractor-api/.env
